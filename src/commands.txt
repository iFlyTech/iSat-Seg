# Commands to run basiss.py

# run in the bassis docker container
# create container
	nvidia-docker build -t basiss path_to_basiss/docker
# load container
	nvidia-docker run -it -v /raid:/raid --name bassiss_train ba